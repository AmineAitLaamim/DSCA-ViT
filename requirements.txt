# ============================================================
# DSCA-ViT — Python Dependencies
# ============================================================
# Core runtime dependencies for models/, models_v2/, models_v3/,
# datasets/, utils/, configs/ and the Colab training notebooks.
#
# Python 3.11 recommended (PyTorch 2.x).
#
# Note: the Colab notebooks already include (Cell 2):
#   pip install timm pyyaml scipy seaborn --quiet
# torch / torchvision / numpy / sklearn are pre-installed on Colab.
# This file is for local / reproducible environments.
# ============================================================

# Deep learning
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0

# Configuration
PyYAML>=6.0

# Image loading
Pillow>=9.0.0

# Plotting / visualization (diagnostic notebooks)
matplotlib>=3.7.0
seaborn>=0.12.0